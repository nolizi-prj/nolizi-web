# Code review — kimi

**Change:** `content/products/pumasi-sign.md` — the commons catalog card for
Pumasi Sign, re-measured after `sign.pumasi.ai` deployed its own landing page
at 2026-09-01 01:02:16 UTC.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** kimi, `moonshotai/kimi-k3` via `tools/recruit.sh -f kimi -p -`,
2026-09-01 03:02 UTC.
**Recruitment order:** claude (skipped — builder) → gemini → grok (402, see its
transcript) → qwen → glm → **kimi**.

Same prompt as the gemini, qwen and glm reviews: the full diff, the full
resulting file, the packet's seven exit criteria, the builder's live
measurements, and questions A–F.

**This review, like glm's, ran against the text before the three post-review
corrections listed in `*-code-GATE.md`.** None of those corrections adds a
claim; two narrow a causal statement about issue-close timestamps and one fixes
a `grep` flag pair.

---

## Review of `content/products/pumasi-sign.md` (pumasi-web)

**Per exit criterion:**

**1. "Never deployed" removed; deploy stated with UTC timestamp — MET.** The gate row now reads "**NOT MET** — both surfaces are live as of 2026-09-01…" and the prose states "Surface B … is **live**, deployed at **2026-09-01 01:02:16 UTC**, which closed issue #8 (2026-09-01 00:59:11Z)". The old "merged on `main` and not deployed" passage is deleted.

**2. Bundle filenames — MET.** The only bundle names in the file are `index-CnoFAC2c.js`, `LandingView-C5khdw3s.js`, and `SignedOutView-C2J9s3yp.js` — exactly the three the evidence shows being served. The stale `index-j38Qwibz.js` and `SignedOutView-Cw4c846h.js` appear only in removed lines. The kept names sit under the "Verified on the live host … 2026-09-01 02:40 UTC" stamp; `LandingView-C5khdw3s.js` additionally carries "fetched 2026-09-01 02:40 UTC" inline.

**3. Issue #7 / `/api/auth/login` 404 — MET.** Limitation states #7 "**closed 2026-09-01T00:59:12Z**" with repair "reached users in the deploy at **2026-09-01 01:02:16 UTC**". The card explicitly defuses the 404: "`GET /api/auth/login?next=%2F` … **still answers `404`** — and that is now correct behaviour rather than a defect. Nothing targets it any more."

**4. Stage 1 gate NOT MET, current reason — MET.** Row: "the gate is held on whether Surface B is *honestly calibrated*, and one of its claims is untrue". Prose: "It is now **Surface B is live and one of its claims is untrue** — the licence row". Matches the packet's required reason verbatim.

**5. No self-grant of Apache-2.0; disclosure; sentence survives — MET.** The required sentence "**this page states no licence until it lands**" is retained, immediately followed by "it reports the deployed page's claim rather than repeating it". The disclosure is made ("the product's own landing page went live carrying `Apache-2.0` in three separate claims … Against that, `…/contents/LICENSE` returns **404**").

**6. No test count / gate figure — MET.** The card states "**No test count is quoted here.**" Checked the full file for suite counts (62 is absent), gate percentages, and grep totals. Numbers present are timestamps, HTTP codes, byte sizes of dated fetches, issue/Q numbers, and the "three" Apache-2.0 occurrences — all measured evidence, not suite/gate metrics. The retained "Pure-core suite passes 100%" is the register's verbatim criterion text (present pre-change; the packet's criterion 1 requires the gate row retained), not a measured figure. "All four jobs" describes CI workflow structure, also pre-existing.

**7. Scope — MET.** Diff touches only `content/products/pumasi-sign.md`.

**Answers to the explicit questions:**

**A.** No. Every Apache-2.0 mention is attributed to the deployed page and simultaneously denied. Pointed lines: the heading itself — "About the licence, and what this page **will not say**"; "This card's summary line used to end **'Apache-2.0.'** That is not a claim this page can make today."; "**This card reports the claim; it does not repeat it**". The description field reads "Alpha; no licence file yet." Nothing asserts the grant on the card's own account.

**B.** No live-defect or undeployed assertions remain. The two residual strings — the release-note quote "It is merged and it is **not deployed**" and STAGE.md's "has never been deployed" — are each explicitly framed as historical/stale: "That note is written in the present tense … the deployment has since overtaken it" and "STAGE.md's own gate *table* still carries its pre-deploy wording … This card follows the parts that show the measurement."

**C.** No. The card calls the 404 "correct behaviour rather than a defect" and explains the defect was the button targeting it, now fixed.

**D.** No stale filename; each kept filename is under the 02:40 UTC verification block. See criterion 2 above.

**E.** None found. The builder's own 62-pass result is not quoted; no suite total or gate percentage appears.

**F.** All checkable claims match the supplied evidence: issue close timestamps (#7 00:59:12Z, #8 00:59:11Z), deploy timestamp, chunk names/bytes (`LandingView-C5khdw3s.js`, 10 046 bytes), the `ml(e){return\`/login?next=\`…}` helper, grep-0 for the old path, `/login?next=%2F` → 200 text/html, LICENSE contents API → 404, and the corrected org-wide `licenseInfo` finding (apache-2.0 for tunnel/web, null for sign). No internal contradiction introduced; the "literal words read met / register declines" passage is coherent and explicitly disclosed. Claims beyond the measured evidence (e.g. "five evaluations", `3edd06f` register wording, `2ab3a4f` catalog re-read) are repo-read facts, none contradicted by anything cited.

Build and tests were run clean by the builder (`npm run build` exit 0; `npm test` 62/0).

VERDICT: APPROVE
