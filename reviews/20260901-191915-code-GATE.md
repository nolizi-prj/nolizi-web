# Review gate — the tunnel card, 2026-09-01 19:19 UTC round

**GATE: PASS.** Three non-builder families read one frozen diff and all three
returned `VERDICT: APPROVE`.

| Family | Route | Answered | Verdict |
|---|---|---|---|
| gemini | `agy -p` | 19:20 UTC | **APPROVE** |
| qwen | `openrouter -m qwen/qwen3-max` | 19:19 UTC | **APPROVE** |
| glm | `openrouter -m z-ai/glm-4.6` | 19:20 UTC | **APPROVE** — terse (378 bytes) but answers every lettered question and carries a verdict; it is a transcript, not a husk |
| claude | — | — | *skipped: builder family* |
| grok | — | — | *not launched: HTTP 402 at the two previous rounds* |

CHARTER Part 3 requirement 3 asks for a family other than the builder's. It got
three. Below `launched` the review is advisory (Part 0); it was run anyway,
because this card is the public surface for a product whose own `STAGE.md` §7
lists what a page may and may not say, and a second family reading for the
merged/undeployed split is cheap insurance against the L-009 failure.

## What was frozen, and the proof it stayed frozen

```console
$ git diff origin/main..HEAD | sha256sum    # 19:18:25 UTC, at f75e255, before any launch
1efee40339b6f29611c7299125d5cc555a1a13747233afd0271f9dd2eb5e96ac
$ wc -c /tmp/frozen.diff
31578
$ npm run build                             # exit 0, 36 files
$ npm test                                  # 62/62 pass

$ git diff origin/main..HEAD | sha256sum    # 19:20 UTC, after the last verdict landed
1efee40339b6f29611c7299125d5cc555a1a13747233afd0271f9dd2eb5e96ac
```

The content commit `f75e255` was made before the round was launched, so the
reviewed range is the committed range; this record is a second commit on the
same branch and changes nothing under `content/`.

## What the round confirmed, in the reviewers' terms

- **A — does any sentence let a skimmer think ownership, durability or the
  scheme fix is live on `pumasi.link`?** None, unanimously; gemini and qwen
  each quote the pairing in the front matter.
- **B — any claim unsupported by the measurements?** None. qwen and gemini
  checked the flag name, the token minimum, the TTL and cap, the corrected
  `core/route.go:311`, and `relay/reservation_test.go:640` against the
  measurement list.
- **C — any gate figure, run count or failure rate?** None; gemini lists the
  four withdrawn figures the diff removes.
- **D — any deadline, deployer or DECISIONS position?** None.
- **E — any `BACKLOG.md` item number?** None; gemini notes the two the diff
  removed.
- **F — the `limitation:` front matter** leads with the `https://` announce on
  `pumasi.link` and carries the commit's own limit (loss removed, restart not
  shortened, empty flag is the old relay). All three: yes.
- **G — the sentence on `9cc9e65`'s missing code-round review** is fair as
  written, all three, because it names the charter's advisory rule beside it.
- **H — anything else citable?** None from any family.

No objection was raised, cited or uncited, so nothing was edited after the
freeze and nothing is carried forward from this round.
