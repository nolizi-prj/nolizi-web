# Code review — glm

**Change:** `content/products/pumasi-sign.md` — the commons catalog card for
Pumasi Sign, re-measured after `sign.pumasi.ai` deployed its own landing page
at 2026-09-01 01:02:16 UTC.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** glm, `z-ai/glm-5.3` via `tools/recruit.sh -f glm -p -`,
2026-09-01 02:59 UTC.
**Recruitment order:** claude (skipped — builder) → gemini → grok (402, see its
transcript) → qwen → **glm** → kimi.

Same prompt as the gemini and qwen reviews: the full diff, the full resulting
file, the packet's seven exit criteria, the builder's live measurements, and
questions A–F.

**Two points from this review were acted on by the builder — see
`*-code-GATE.md` for the disposition of each.**

---

# Cross-family review — `pumasi-web` · `content/products/pumasi-sign.md`

Scope confirmed: the diff touches exactly one file in `pumasi-web` (criterion 7). Build exit 0, tests 62 pass / 0 fail — and, as checked under E, the card quotes none of those numbers.

## Answers to the explicit questions

**A. Does the card state or imply, on its own account, that Pumasi Sign is Apache-2.0?**
No. Every Apache-2.0 mention is framed as one of four things, none of them a grant by this card:

- a report of the deployed page's claim — "it tells a stranger, three times, that the code is under Apache-2.0 — and the repository grants no licence at all"; "`Apache-2.0` appears in the hero strip, in the stage banner, and in a comparison-table row headed *License & Source Code*";
- the decision file's pending options — "named default is to add the Apache-2.0 file and honour the claim, and whose named alternative is to remove the claim instead";
- fact about *other* repos — "`pumasi-tunnel`, Apache License 2.0, as does this website's own repository" (supported by the re-measured `licenseInfo` finding);
- this card's own retracted history — "This card's summary line used to end **'Apache-2.0.'** That is not a claim this page can make today."

The card's own account is the denial, twice, grounded in the re-measured 404: "a public repository with no licence grants no rights at all." The required sentence survives verbatim: "**this page states no licence until it lands** — it reports the deployed page's claim rather than repeating it." The frontmatter description ends "no licence file yet." Criterion 5 met.

**B. Any remaining assertion that Surface B is undeployed, or that #7 is live?**
No, not in the card's own voice. The card states the deploy with its UTC timestamp in every relevant place ("deployed at **2026-09-01 01:02:16 UTC**"; "**The entry-path defect this card warned about for two days is gone.**"). Two residual strings are attributed quotations, not assertions: STAGE.md's stale gate-table wording ("Surface B *'has never been deployed'*"), explicitly disclosed as that file's internal inconsistency with "The row is that file's to correct, not this one's"; and the release note's first line, immediately followed by "the deployment has since overtaken it." The CI passage is past-tense ("Four green jobs **sat** above a live entry-path defect… not repaired for users until the deploy at 2026-09-01 01:02:16 UTC"). Criteria 1 and the #7 half of 3 met.

**C. Does the card call the `/api/auth/login` 404 a defect?**
No — it says the opposite, explicitly and in the packet's own terms: "`GET /api/auth/login?next=%2F` on that host **still answers `404`** — and that is now correct behaviour rather than a defect. Nothing targets it any more." This matches the evidence line for line.

**D. Stale or undated bundle filenames?**
Three bundle names appear, all served per the 02:40 evidence: `/assets/LandingView-C5khdw3s.js` ("fetched 2026-09-01 02:40 UTC", explicit), and `/assets/index-CnoFAC2c.js` / `SignedOutView-C2J9s3yp.js`, both inside the passage headed "**Verified on the live host rather than on the tracker**, 2026-09-01 02:40 UTC:". The stale chunks (`index-j38Qwibz.js`, `SignedOutView-Cw4c846h.js`, and the 839 941-byte reading) are removed everywhere — I checked the full new file, not just the diff hunks. Criterion 2 met.

**E. Test count or gate figure quoted?**
None. No test count appears (the card restates the discipline: "**No test count is quoted here.**"); the 62/36 figures appear nowhere. The only number in the gate table is the criterion text "Pure-core suite passes 100%", which is STAGE.md's own *definition* of the Alpha gate, carried unchanged from the previous version (identical in the − and + rows) and presupposed by exit criteria 1 and 4, which direct what the row's *status* cell must say. It is not a measured or aging figure. No pass counts, no "X of Y" gates. Criterion 6 met.

**F. Unsupported claims or introduced contradictions?**
Every load-bearing claim matches the re-measured evidence: served chunks and sizes (10046 bytes), Apache-2.0 ×3, BETA ×0, the `ml(e)` helper, `/login?next=%2F` → 200 text/html, `/api/auth/login` → 404, #7 CLOSED 00:59:12Z, #8 CLOSED 00:59:11Z, LICENSE → 404, the deploy timestamp, and the `licenseInfo` correction (the old false "null for every repo" assertion is now time-bounded to "Through 2026-08-31" and explicitly corrected — exactly what the packet's NEW FINDING required). Criterion 4's reason is stated verbatim: "It is now **Surface B is live and one of its claims is untrue**."

Beyond the enumerated evidence, the edit adds several claims resting on the builder's reading of the fetched 10 KB landing chunk and of repository files. None is contradicted by any evidence, none is load-bearing for criteria 1–7, so I record them as observations, not objections:

1. Placement of the three Apache-2.0 strings (hero strip, stage banner, comparison row) — detail beyond "×3", but the card quotes the chunk verbatim elsewhere (the stage constant), so the chunk's contents were plainly read, and the count matches.
2. "an uncited competitor-pricing block, which shipped cited and dated" — not in the evidence list; consistent with the same chunk reading; the paired claim (BETA shipped reading ALPHA) is evidenced.
3. "no user has ever seen a `BETA` badge" — established for the landing page (first deploy 01:02:16 with BETA ×0; the chip's only home was `LandingView.vue`); the wider wording is the scope of the two-day argument, which the same sentence confines to "a page nobody could yet open."
4. "answered `200` on `/api/health` when this card was written (2026-09-01 02:40 UTC)" — the /api/health check is **not** among the enumerated 02:40 measurements. The claim is inherited (the unchanged "Inside the Product" section still carries it, previously verified down to the response body), and this edit's change is an improvement in hygiene (past tense + date), but the builder should have this in the measurement log. Incidental to every exit criterion; not a clause failure.
5. Hash-attributed repo readings: STAGE.md@`3edd06f`'s self-inconsistent table, the `0e26917` fingerprint identification, `2471a29` moving `main` past the deploy, catalog.json@`2ab3a4f` "three days stale" (arithmetic checks: 08-29 → 09-01), the "frozen case" build gate, "Through five evaluations". All attributed, all consistent with the packet's own account — criterion 4's "current reason" presupposes the register holds the gate NOT MET after the deploy.
6. No internal contradiction introduced: gate row, prose, limitation, and deployment bullet all tell one story, and the one apparent tension ("On the gate's literal words the row now reads met; the register declines to record it as met…") is surfaced and argued in the card rather than hidden. (Trivial note: `grep -oc` is a nonstandard flag pair — `-c` overrides `-o` — but the claimed result, `0`, matches the measured BETA ×0 either way.)

## Exit-criteria tally

1. Undeployed assertions removed, deploy dated (01:02:16 UTC) — met.
2. Only currently-served bundle names, all fetch-dated — met.
3. #7 CLOSED 00:59:12Z, no live-defect claim, 404 explicitly not a defect — met.
4. NOT MET with the current reason, stated verbatim — met.
5. No own-account licence claim; deployed claim disclosed as a claim; "this page states no licence until it lands" survives — met.
6. No test counts or gate figures — met.
7. One file, in `pumasi-web` — met.

No cited objection stands: every check I could run against the packet's evidence, clauses, and diff lines passes, and the observations above fail no clause of the spec.

VERDICT: APPROVE
