# Code review — glm

**Change:** `content/products/pumasi-booking.md` and
`content/products/pumasi-sign.md` in `pumasi-web`, against `cd1fd2f`.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** glm, via `openrouter -m z-ai/glm-4.6 -p -`, prompt on stdin.
**Launched** 2026-09-01 05:51:59 UTC, **answered** by 05:53:48 UTC, exit 0.
**Recruitment order:** claude (skipped — builder) → **gemini** → **qwen** →
**glm** → grok (UNREACHABLE, HTTP 402; not launched, per the packet).

**The diff this reviewer read is provably the diff that was committed.**
The tree was frozen at 05:51:03 UTC — `npm run build` clean (36 files,
exit 0), `npm test` 62/62 — and the diff checksummed:

```console
$ sha256sum /tmp/frozen.diff
f15c9c7e7f6d3bf53d9402361a55bb7dab9d57df63bb5012229ca14d4002101f
$ wc -c /tmp/frozen.diff
43083
```

That checksum was written into the prompt every reviewer received, and the
tree was **re-checksummed after the last verdict landed and still matched**.
No file was edited between the freeze and the commit.

**Prompt contents:** the frozen diff; the nine live-host and source
measurements this seat took between 05:48:45 and 05:50:00 UTC, itemised with
timestamps; the six hard constraints the change is under; and seven lettered
questions — A being whether any sentence lets a skimming reader conclude a
repair has reached users, E whether the re-measured commit count is honest
or noise, and F whether the booking section is proportionate.

**Mechanical note.** Raw capture contained **0 NUL bytes**. Trailing
whitespace normalised; **no word of the review was changed, added or
removed.**

---

A. No. Every repair is explicitly paired with its undeployed status. Examples:
- "the repair is merged and it is not deployed" (booking card, limitation field and new section)
- "It is merged and it is **not deployed**" (sign card, limitation field for both repairs)
- "this page does not claim that repair has shipped" (booking card, new section)

B. No. The text explicitly avoids setting deadlines, naming deployers, or taking positions:
- "this card sets no deadline, names no deployer, and takes no position on any default" (booking card)
- "this card sets no deadline on any of those entries, names no deployer, and takes no position on their defaults" (sign card)
- DECISIONS entries are reported with their dates (e.g., Q-038 window closing 2026-09-08) but framed as the entry's own, not the card's.

C. No. All claims align with measurements:
- Booking live commit `2453adc` and missing repairs `d7bd490`/`c000feb` match `git rev-list` output (8 commits ahead, repairs not ancestors).
- Sign live tree `0e26917` and missing repairs `9659e69`/`2471a29` match chunk fingerprints and `git rev-list` (9 commits ahead).
- License API 404 for `pumasi-sign` and Apache-2.0 for others match `gh api` calls.

D. Defensible. The empty booking page is a critical, user-facing defect that blocks core functionality (no bookings can occur), whereas the reminder issue is secondary. Leading with the higher-impact defect is appropriate.

E. Honest and useful. It emphasizes volatility ("run the command rather than trusting the number") and provides a reproducible check, preventing staleness. The specificity (e.g., "05:49 UTC") adds credibility.

F. Proportionate. The section details a severe defect with concrete measurements (curl, headless Chrome) and deployment context without inflation. The length matches the impact: a broken core feature with merged fixes waiting.

G. No material issues. The text is precise, well-supported, and compliant with all constraints. The level of detail is justified by the defects' severity.

VERDICT: APPROVE
