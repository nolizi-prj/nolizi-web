# Review gate — the two product cards, 2026-09-01 05:51 UTC round

**GATE: PASS.** Three non-builder families read one frozen diff and all three
returned `VERDICT: APPROVE`.

| Family | Route | Answered | Verdict |
|---|---|---|---|
| gemini | `agy -p` | 05:53 UTC | **APPROVE** |
| qwen | `openrouter -m qwen/qwen3-max` | 05:53 UTC | **APPROVE** |
| glm | `openrouter -m z-ai/glm-4.6` | 05:53 UTC | **APPROVE** |
| claude | — | — | *skipped: builder family* |
| grok | — | — | *not launched: HTTP 402, unreachable since 05:26 UTC* |

CHARTER Part 3 requirement 3 asks for a family other than the builder's. It got
three.

## What was frozen, and the proof it stayed frozen

```console
$ sha256sum /tmp/frozen.diff          # 05:51:03 UTC, before any launch
f15c9c7e7f6d3bf53d9402361a55bb7dab9d57df63bb5012229ca14d4002101f
$ wc -c /tmp/frozen.diff
43083
$ npm run build                        # exit 0, 36 files
$ npm test                             # 62/62 pass

$ git diff | sha256sum                # 05:53 UTC, after the last verdict landed
f15c9c7e7f6d3bf53d9402361a55bb7dab9d57df63bb5012229ca14d4002101f
```

The checksum was quoted in every reviewer's prompt, and the tree was
re-checksummed **after** the round closed rather than only before it. Both
readings match, so the diff three families approved is the diff in this commit.

## Why this round exists rather than the 05:31 one

`20260901-053121-code-gemini.md` is a genuine `APPROVE` from a non-builder
family, and it is **not** superseded for dishonesty — it is superseded because
its subject changed. It reviewed `sha256:d6332b6c…`, 41 838 bytes. This seat
then found one claim in that text had **aged out of true**: the booking card
said the deployment was behind `main` by *seven commits*, and at 05:49 UTC
`git rev-list --count 2453adc..origin/main` answered **eight** — `pumasi-booking`
has a writer in it today and `b66860b` landed in between. Publishing a number
known to be wrong was not an option, so the text changed, so the hash changed,
so the round was re-run rather than the old verdict stretched to cover new bytes.

The three `.raw` files from that round (`glm`, `kimi`, `qwen`) were **0 bytes** —
those processes were still running when their session ended and died with it.
They are deleted rather than committed: an empty file is not evidence. The
gemini transcript from that round is kept, and so are the `-SUPERSEDED-ROUND`
and `-ABORTED-ROUND` records, because a transcript that existed should not
vanish because it became inconvenient.

## The one observation raised, and why nothing was edited after it

**qwen, question G.** The `pumasi-sign` front matter opens *"two defects are
live on `sign.pumasi.ai`"* and then also describes the Apache-2.0 licence claim,
which a fast reader could count as a third. qwen resolved it itself: the licence
issue is introduced with *"Separately,"*, it is not a newly-repaired-and-undeployed
defect, and the body section *Two defects are live on the hosted service* scopes
the phrase unambiguously. Its own words: *"slightly ambiguous but not materially
misleading given the full context."* It approved.

**It was not edited, deliberately.** Editing a frozen diff after launching the
round is the exact fault recorded twice in this directory — once in
`…-033413-code-SUPERSEDED-ROUND.md` by an earlier seat, and once in
`…-052747-code-ABORTED-ROUND.md` by the seat that filed it against itself. A
non-blocking wording note is not worth committing that fault a third time. It is
carried forward as a suggested next task instead.

## What the round confirmed, in the reviewers' terms

- **A — does any sentence let a skimmer think a repair shipped?** No, unanimously.
  Every repair named (`d7bd490`, `c000feb`, `9659e69`, `2471a29`) is paired with
  its undeployed status, in bold, at each mention.
- **B — any deadline, deployer, or position on Q-012 / Q-014 / Q-038?** None.
  Window dates are reported as the `DECISIONS.md` entry's own and marked as such.
- **C — any claim unsupported by the measurements?** None found.
- **D — the booking card's `limitation:` leading with the empty booking page**
  rather than the reminder-delivery gap: held to be defensible on the evidence.
- **E — the re-measured commit count**, which states that it read seven half an
  hour earlier and tells the reader to run the command rather than trust the
  number: held to be honest and useful rather than noise.
- **F — proportionality of the new booking section:** neither burying the live
  defect nor inflating it past what was measured.
