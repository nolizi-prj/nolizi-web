# Code review — qwen

**Change:** `content/products/pumasi-tunnel.md` and one sentence of `content/products/pumasi-sign.md` in `pumasi-web`, commit `f75e255` against `origin/main` `1b8602a` (PR #21).
**Builder family:** claude (`claude-fable-5-1`) — reviewer is a different family, per CHARTER Part 3 requirement 3.
**Reviewer:** qwen, via `openrouter -m qwen/qwen3-max -p -`, prompt on stdin.
**Launched** 2026-09-01 19:19:15 UTC, **answered** by 19:20:05 UTC, exit 0, 2196 bytes.
**Recruitment order:** claude (skipped — builder) → gemini → qwen → glm → grok (not launched: HTTP 402 at the two previous rounds, not re-probed).

**The diff this reviewer read is the diff that was committed.** Frozen at 19:18:25 UTC as `git diff origin/main..HEAD` at `f75e255`: `sha256 1efee40339b6f29611c7299125d5cc555a1a13747233afd0271f9dd2eb5e96ac`, 31 578 bytes; `npm run build` clean (36 files), `npm test` 62/62. Re-checksummed after the last verdict landed: identical. No file was edited between the freeze and this record.

**Prompt:** the frozen diff; the live-host and source measurements this seat took at 19:12 UTC and against `pumasi-tunnel` `9cc9e65` / `pumasi` `23bbc64`, itemised; seven hard constraints; eight lettered questions A–H (skimmer-safety of the merged/undeployed split, support by measurement, gate figures, DECISIONS positions, backlog numbers, the `limitation:` front matter, fairness of the review-record sentence, anything else citable). Verdict line required.

---

## Transcript, verbatim

A. No. Every sentence explicitly contrasts pumasi.link (where names belong to nobody and restarts drop everything) with main (where fixes exist but are undeployed). Example: "**On `pumasi.link` a name still belongs to nobody and a restart still drops every name...** Both are fixed on `main` and in the same merged-and-undeployed state..."

B. None. All factual claims align with measurements:
- pumasi.link status lacks "reserved" key (measurement confirms; diff cites this as evidence of pre-4489fbe build).
- -reservations flag off by default (cmd/pumasi-relay/main.go line 46 confirmed in measurements).
- PublicURL line number corrected from 312→311 (core/route.go measurement confirms line 311).

C. No. The diff explicitly avoids quoting figures: "*this page does not repeat them*" and "*nothing public quotes its figure*". Earlier failure rate discussion is replaced with: "*An earlier version of this page quoted a failure rate... It no longer does*".

D. No. Q-014/Q-024 are mentioned only as open DECISIONS.md entries without positions (e.g., "*Q-014, which is open*"). No deadlines/deployers named.

E. No. BACKLOG.md is referenced generally ("*entry rather than an opinion*") but no item numbers cited (constraint 5 satisfied).

F. Yes. Limitation leads with pumasi.link's critical constraints (http-only, undeployed fixes). Correctly states reservation file removes *loss* from restarts but doesn't shorten restart duration ("*does not make one shorter*"), matching core/reservationstore.go behavior where only claims persist, not live connections.

G. Fair as written. States factually: "*no code-round [review transcript], and no Reviewed-By trailer*" and contextualizes charter compliance: "*Below launched the charter makes that review advisory, so the merge is in order*". No accusatory language.

H. None. All technical details match measurements:
- Flag name "-reservations" correct (cmd/pumasi-relay/main.go line 46).
- Token length ≥16 (core/reservation.go MinTokenLen=16).
- Reservation TTL 30 days, max 10k reservations (core/reservationstore.go).
- PublicURL line 311 corrected.
- TestAReservationOutlivesTheRelay at relay/reservation_test.go:640.

VERDICT: APPROVE
