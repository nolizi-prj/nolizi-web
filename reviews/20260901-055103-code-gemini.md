# Code review — gemini

**Change:** `content/products/pumasi-booking.md` and
`content/products/pumasi-sign.md` in `pumasi-web`, against `cd1fd2f`.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** gemini, via `agy --dangerously-skip-permissions -p`, prompt passed as an argv string.
**Launched** 2026-09-01 05:51:59 UTC, **answered** by 05:53:48 UTC, exit 0.
**Recruitment order:** claude (skipped — builder) → **gemini** → **qwen** →
**glm** → grok (UNREACHABLE, HTTP 402; not launched, per the packet).

**The diff this reviewer read is provably the diff that was committed.**
The tree was frozen at 05:51:03 UTC — `npm run build` clean (36 files,
exit 0), `npm test` 62/62 — and the diff checksummed:

```console
$ sha256sum /tmp/frozen.diff
f15c9c7e7f6d3bf53d9402361a55bb7dab9d57df63bb5012229ca14d4002101f
$ wc -c /tmp/frozen.diff
43083
```

That checksum was written into the prompt every reviewer received, and the
tree was **re-checksummed after the last verdict landed and still matched**.
No file was edited between the freeze and the commit.

**Prompt contents:** the frozen diff; the nine live-host and source
measurements this seat took between 05:48:45 and 05:50:00 UTC, itemised with
timestamps; the six hard constraints the change is under; and seven lettered
questions — A being whether any sentence lets a skimming reader conclude a
repair has reached users, E whether the re-measured commit count is honest
or noise, and F whether the booking section is proportionate.

**Mechanical note.** Raw capture contained **0 NUL bytes**. Trailing
whitespace normalised; **no word of the review was changed, added or
removed.**

---

### Cross-Family Review: `pumasi-web` Catalog Cards (`cd1fd2f`)

**Reviewer Context:** Cross-family review under Pumasi CHARTER Part 3, requirement 3. Evaluated strictly against measured evidence, repository commits, and hard constraints.

---

### A. Reader Impression of Undeployed Repairs

> **Question:** Does ANY sentence let a skimming reader conclude that a repair named (`d7bd490`, `c000feb`, `9659e69`, `2471a29`) has reached users?

**No.** Every occurrence of each repair is explicitly paired with its undeployed status, the deployed SHA serving the live host, and the live reproduction of the defect.

* **`d7bd490` (Booking empty times):**
  * *Front matter:* `"repaired at [`d7bd490`](...), and **not deployed**: `/version` answered `{"version":"0.2.0","commit":"2453adc"}` at 05:20:31 UTC and the repair is not in that build."*
  * *Section header:* `"## The hosted booking page shows no times, and the repair is merged and not deployed"`
  * *Section body:* `"The repair is merged and it is not deployed... **This page does not claim that repair has shipped.** `curl https://booking.pumasi.ai/version` answered `{"version":"0.2.0","commit":"2453adc"}` at **05:20:31 UTC** on 2026-09-01, and the repair is not in that build..."*
  * *Comparison table:* `"`d7bd490` is not an ancestor of `2453adc`; measured on the host 2026-09-01 05:21, first section on this page"` and `"the two live defects above are reviewed, gate-passed, merged, and not in this build."`
* **`c000feb` (Booking Subprocessor Register / Zoom):**
  * *Front matter:* `"`main` names *Zoom Video Communications, Inc.* since `c000feb`; the deployment does not, and nothing carries the correction across"`
  * *Section body:* `"The repair is merged and not deployed. `pumasi-booking` [`c000feb`](...) makes `service/src/legal.ts` name **Zoom Video Communications, Inc.** ... `git show 2453adc:service/src/legal.ts | grep -ci zoom` returns **`0`**, so the copy now in production never carried it."`
  * *Comparison table:* `"names five parties and not Zoom, and the build sends Zoom the booker's name and email"`
* **`9659e69` (Sign settings pencil deletes message):**
  * *Front matter:* `"two defects are live on `sign.pumasi.ai` as this is written, both repaired in `main` and neither deployed... Repaired at [`9659e69`](...)"`
  * *Section body:* `"Merged, and not deployed. [`9659e69`](...) is in `main` and tells *absent* (keep) from *present-and-null* (clear)... **This page does not claim that repair has shipped**..."`
  * *Queue section:* `"Q-037 counted three repairs already waiting in that undeployed bundle and this is the fourth."`
* **`2471a29` (Sign expiration dates bind):**
  * *Front matter:* `"Repaired at [`2471a29`](...), [release note](...) — **Q-035**... **Both are merged and undeployed, and this page does not claim either has shipped**"`
  * *Section body:* `"Merged, and not deployed. [`2471a29`](...) adds the hourly cron trigger... and its own second paragraph reads *It is merged and it is **not deployed***."`
  * *Queue section:* `"Q-035: 'Q-028 records that two earlier repairs already wait in the same undeployed bundle; this is the third.'"`

---

### B. Governance Deadlines, Deployers, and Defaults

> **Question:** Does the change set any deadline, name any deployer, or take a position on Q-012 / Q-014 / Q-038's default?

**No.** The diff sets no deadlines, names no deployer, takes no stance on defaults, and explicitly notes that all window dates are quotes from `DECISIONS.md`.

* *Booking card on Q-012 & Q-038:*
  > `"Why it is still here. Nobody owns deployment. That is DECISIONS.md Q-012, open, and explicitly outside CHARTER Part 0's proceed-on-default rule, so unlike nearly everything else in this project an agent may not proceed on it... The entry is Q-038, whose own row records a window closing 2026-09-08 — that is the entry's date, reported here and not set here. This card sets no deadline, names no deployer, and takes no position on any default."`
* *Booking card on Q-012 & Q-036:*
  > `"`pumasi/DECISIONS.md` **Q-012** and **Q-036**, both open, and neither this page's to answer."`
  > `"Whether that clause was owed for a provider that reached production undisclosed... is open as DECISIONS.md Q-036 and is the steward's. This card reports that entry's state and takes no position in it."`
* *Sign card on Q-012, Q-021, Q-035, Q-037:*
  > `"That date is DECISIONS.md Q-037's own, reported here and not set here — this card sets no deadline on any steward entry and has no standing to."`
  > `"Nobody owns deployment. That is DECISIONS.md Q-012, open — and explicitly outside CHARTER Part 0's proceed-on-default rule... This card sets no deadline on any of those entries, names no deployer, and takes no position on their defaults."`
  > `"Someone deployed once; the question of who owes you the next one is still open."`

---

### C. Verification of Factual Claims Against Measurements

> **Question:** Is any factual claim unsupported by the measurements listed above, or contradicted by them?

**No.** All factual claims, byte counts, timestamps, commit hashes, and reproduction steps match the recorded measurements:

1. **Booking version & status:** `/version` returned `{"version":"0.2.0","commit":"2453adc"}` at `05:20:31 UTC` and `05:48:45 UTC` (matches `pumasi-booking.md:46`, `pumasi-booking.md:92`).
2. **Booking slots payload & DOM behavior:** `GET /yunyoungmok/abc` returned `200`, `45 407` bytes, 24 slots in `#slots-data`, `times.querySelectorAll` (1), `times.textContent` (1). Headless Chrome at `05:21:26 UTC` and `05:49:38 UTC` showed days 1 & 2 available, `#times.children.length === 0` under both days, 0 console/page errors (matches `pumasi-booking.md:46-59`, `pumasi-booking.md:86-95`).
3. **Booking subprocessors:** `/subprocessors` answered `200`, `34 663` bytes, 5 named parties; `grep -ic zoom` returned 4 presentation matches, `grep -io "zoom video"` returned 0 (matches `pumasi-booking.md:448-452`, `pumasi-booking.md:493-502`).
4. **Booking commit divergence:** `git rev-list --count 2453adc..origin/main` was 7 at `05:21 UTC` and 8 at `05:49:35 UTC` (`origin/main = b66860b`, containing `d7bd490` and `c000feb`; matches `pumasi-booking.md:192`).
5. **Sign deployment & health:** Root loads `/assets/index-CnoFAC2c.js` (`0e26917`); `/api/health` returned `200`; chunk byte counts (`ExternalSignView-CmDSqiPb.js` 5,250 bytes, `SignView-BM1dkPaZ.js` 46,980 bytes, `LandingView-C5khdw3s.js` 10,046 bytes) match `05:50:00 UTC` measurements (`pumasi-sign.md:188-195`).
6. **Sign LICENSE status:** `gh api repos/pumasi-ai/pumasi-sign/contents/LICENSE` returned 404 at `05:22:52 UTC` / `05:49:57 UTC`; `licenseInfo` returned `null` for `pumasi-sign` and `apache-2.0` for `pumasi-tunnel` and `pumasi-web` at `05:26:39 UTC` (`pumasi-sign.md:423-435`).
7. **Sign commit divergence:** `git rev-list --count 0e26917..f0d1912` = 9 commits at `05:49:35 UTC` (`pumasi-sign.md:509-511`).

---

### D. Ordering in Booking `limitation:` Front Matter

> **Question:** The booking card's `limitation:` front matter now LEADS with the empty booking page rather than the reminder-delivery gap. Is that ordering defensible on the evidence, or is it overstated?

**The ordering is defensible and accurate.**
* **User interaction sequence:** A visitor landing on `booking.pumasi.ai` immediately hits the slot selection UI. Because `#times.children.length === 0`, no booking can be created.
* **Defect impact comparison:** The reminder delivery gap pertains to post-booking execution (and the repair timer is already deployed on the worker as of `00:40:44 UTC`). The empty slot renderer defect is currently undeployed, completely blocks the entry point of the entire application, and fails silently. Putting the primary blocker first reflects user reality.

---

### E. Dynamic Commit-Count Framing

> **Question:** Is the new commit-count sentence ("eight commits at 05:49 UTC ... It read seven half an hour earlier, at 05:21; run the command rather than trusting the number") honest and useful, or is it noise that will itself go stale?

**Honest and useful.**
* Rather than presenting a static count as an immutable fact, the sentence explicitly illustrates that `origin/main` is under active development (advancing from 7 to 8 commits within 30 minutes) while the deployment remains stationary.
* It explicitly provides the verification command (`git rev-list --count 2453adc..origin/main`) and cautions the reader to run the command rather than relying on a point-in-time scalar. This maintains epistemic integrity.

---

### F. Proportion of the Booking Section

> **Question:** Is the new booking section proportionate — neither burying a live defect nor inflating it beyond what was measured?

**The section is proportionate.**
* It accurately documents the exact defect: buttons are generated in memory and dropped because `times.appendChild(b)` was omitted.
* It does not inflate the issue (it explicitly notes that HTTP status is 200, `#slots-data` has 24 valid slots, and zero runtime errors/exceptions are thrown).
* It provides the exact commits involved (`50f911f` deletion, `d7bd490` fix), cites the specific test (`service/test/booking-slots.test.ts`), explains why it remains undeployed (Q-012/Q-038), and clarifies that local clones of `main` build cleanly and work.

---

### G. Material Integrity, Consistency, and Cross-References

> **Question:** Anything else materially wrong, misleading, or self-contradictory.

**None found.**
* Markdown formatting, table structures, and code blocks in both files are intact and clean.
* Cross-references between `pumasi-booking.md` and `pumasi-sign.md` concerning `Q-012` and the queue of undeployed repairs (`Q-028`, `Q-035`, `Q-037`, `Q-038`) are mutually consistent and match the decision log structure.
* Commit citations and links correctly reference the specific SHAs evaluated.

---

VERDICT: APPROVE
