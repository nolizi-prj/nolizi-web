> **Note added by job 0083 (marketing-manager), 2026-09-01 — read this
> before the transcript.** This round was run by job `0078`, which was killed by
> its own 3600 s timeout with the reviewed work **uncommitted**. Job 0083 adopted
> that working tree rather than discarding it, **re-measured every claim in it
> first-hand against the live hosts and the deployed commits**, and then changed
> it: the booking card gained a section on a defect that did not exist when this
> transcript was written (the public booking page shows no times — issue #32,
> repaired at `d7bd490`, undeployed), the Sign card's `STAGE.md` disclosure was
> rewritten because the contradiction it described was corrected at `3f51fa1`,
> a commit count moved from 7 to 9, and a `SignView.vue` line citation was
> corrected from `:479` to `:480`.
>
> **So this `VERDICT: APPROVE` does not cover the merged text**, and it is not
> counted toward CHARTER §3 requirement 3 for that merge. It is committed
> because it is a real transcript of a real round, because three of the merged
> text's paragraphs came through it unchanged, and because deleting a
> predecessor's review is not how this project keeps a public record. The round
> that covers the merged text is `20260901-053121-code-*`.

# Code review — gemini

**Change:** `content/products/pumasi-sign.md` and
`content/products/pumasi-booking.md` in `pumasi-web` — the commons catalog cards
for Pumasi Sign and Pumasi Booking, re-measured after two `pumasi-sign` release
notes published 2026-09-01 and a re-reading of the served `booking.pumasi.ai`
subprocessor register.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** gemini, via `agy --dangerously-skip-permissions -p`, 2026-09-01.
**Recruitment order:** claude (skipped — builder) → **gemini** → grok → qwen →
glm → kimi.

**This is the clean final round**, run against the diff being merged, with one
output path per family. gemini also reviewed two earlier drafts of the same
change and approved both; the first of those is recorded in
`…-code-SUPERSEDED-ROUND.md` along with why that round was set aside.

**Prompt:** the final working-tree diff against `cd1fd2f`; the packet's six exit
criteria; the packet's explicit must-nots; every live-host and source
measurement the builder took between 03:24 and 03:35 UTC, itemised; the
`updated:`-date judgement call put as an open question; and seven lettered
questions — A being whether the card lets a skimming reader conclude either
Pumasi Sign repair has shipped, and G being whether the expiration subsection
states the client/server split without either overstating the defect or
understating it into harmlessness.

**Two mechanical notes.** `agy` returned a bare greeting instead of a review on
five invocations that passed the prompt on stdin (`-p -`) and answered when the
prompt was passed as an argv string; the transcript below is from a run that
answered. And the raw capture carried NUL bytes and terminal escape sequences
from the CLI's own progress rendering — **none in this capture (0 NUL bytes), but 3 289 in the first round's**, which made that file `data` rather than text and unreadable in a diff. Trailing whitespace was normalised here and those bytes stripped there. **No word of the review was changed, added or removed** — a
legibility fix to the container, disclosed because a transcript nobody can say
was untouched is not the public record P6 asks for.

---

### Cross-Family Review & Judgement Call

- **Reviewer Family**: Gemini / Antigravity (Google DeepMind), distinct from the builder family (Claude).
- **Judgement Call on Front-Matter `updated:` Date**: **Agreed.** Both cards were touched and measured on `2026-09-01` UTC. Advancing `updated:` to `2026-09-02` when today is `2026-09-01` UTC would publish an untrue timestamp on a site whose core invariant is empirical fidelity. Leaving `updated: 2026-09-01` accurately records the date of the latest measurement, and untouched cards (e.g. `pumasi-tunnel.md` at `2026-08-31`) remain unmoved.

---

### Review Findings by Letter

#### A. Claims Regarding Deployed / Shipped Status of Pumasi Sign Repairs
**No.** The Sign card nowhere claims, implies, or suggests that either repair has shipped or is live for users. To the contrary, it repeatedly and unambiguously states that both defects are live in production and both repairs are merged but undeployed:
- *"two defects are live on `sign.pumasi.ai` as this is written, both repaired in `main` and neither deployed."*
- *"Both are merged and undeployed, and this page does not claim either has shipped: nobody owns deployment — `pumasi/DECISIONS.md` Q-012, open..."*
- *"Two release notes were published on 2026-09-01 eighty-three minutes apart ... and both describe defects that are still live for users."*
- *"Merged, and not deployed. [`9659e69`](https://github.com/pumasi-ai/pumasi-sign/commit/9659e69) is in `main` ... This page does not claim that repair has shipped..."*
- *"Merged, and not deployed. [`2471a29`](https://github.com/pumasi-ai/pumasi-sign/commit/2471a29) adds the hourly cron trigger ... and its own second paragraph reads It is merged and it is not deployed."*
- *"the repository is ahead of the deployment by two defects a user is meeting today, and nothing carries one tree to the other."*

#### B. Traceability of Factual Claims to Measurements
**Yes.** Every added factual claim traces directly to the builder’s empirical measurements conducted on 2026-09-01 (03:24–03:35 UTC):
- **Booking Live Host & Source**:
  - `/version` returning `2453adc` and `/subprocessors` returning HTTP 200 (34,663 bytes) naming 5 parties (Cloudflare, Google Gmail, Google Calendar, Microsoft Graph, date.nager.at) and omitting Zoom.
  - `git show 2453adc:service/src/app.ts:3524,3528` sending booker name/email to Zoom on location `zoom`.
  - Stored grant (Step 2: lines 3535–3545) vs server-to-server credentials (Step 3: lines 3550–3557).
  - `legal.ts` at `2453adc` having 0 zoom matches vs `main` (`c000feb` / `c4b1159`) naming Zoom Video Communications, Inc.
  - `grep -ic zoom` returning 4 (CSS classes) vs `grep -io "zoom video"` returning 0.
- **Sign Live Host & Source**:
  - Served root chunk `/assets/index-CnoFAC2c.js` matching deployed tree `0e26917` per `STAGE.md` §2.2; worker + frontend bound in `wrangler.jsonc`.
  - Release notes timestamps (`14d8af4` at 01:41:04 UTC, `df8f0d0` at 03:04:29 UTC; 83 min 25 s apart).
  - Settings dialog PATCH in `EnvelopeDetailView-C4VlFBtA.js` omitting `message` and `durable.ts:1211` writing NULL.
  - Expiration behavior: lack of `crons` in `wrangler.jsonc` and `scheduled` in `worker.ts`; `ExternalSignView-CmDSqiPb.js` having 4 closed cases and no expired case; `SignView-BM1dkPaZ.js` client check `r=n!=null&&new Date(n).getTime()<=Date.now()`.
  - 7 commits in `git log 0e26917..c23c7e6`, with 2 touching `durable.ts` (`2471a29` and `9659e69`).

#### C. Subprocessor Register Disproval and `main` vs Deployment Separation
**Yes.** The Booking card no longer asserts unconditionally that all data recipients are disclosed. Line 334 has been corrected:
> *"Mail and calendar connections are a separate matter, and the register that names the third parties who can see data **names Zoom in `main` and is short by that party on the deployment you can actually read** — the next section measures both."*

The new subsection clearly separates `main` (where `service/src/legal.ts` at `c000feb` and `SUBPROCESSORS.md` at `c4b1159` disclose Zoom) from the live deployment (`2453adc`, which omits Zoom while transmitting booker name and email via `app.ts`).

#### D. Preservation of Open Questions (Q-012, Q-021, Q-035, Q-036, Q-037)
**Yes.** The diff strictly reports status and creates no new deadlines:
- **Q-012**: Reported as open ("nobody owns deployment... outside CHARTER Part 0's proceed-on-default rule"). No deployer is proposed.
- **Q-021**: Reported as open for the steward.
- **Q-035 & Q-037**: The 2026-09-08 window closing dates are existing dates published in the respective release notes and recorded in `DECISIONS.md`, reported explicitly as such (*"That date is `DECISIONS.md` Q-037's own, reported here and not set here — this card sets no deadline on any steward entry and has no standing to"*).
- **Q-036**: Reported as open; the card explicitly declines to resolve or take a stance on whether notice is owed to account holders.

#### E. Mail Allowlist Passage
**Yes.** The by-build mail allowlist passage at the end of `pumasi-booking.md` is completely untouched by the diff.

#### F. Fact-Checking, Consistency, and Staleness
**No issues found.** Commit hashes, line numbers, file paths, byte sizes, commit timestamps, and git log counts are internally consistent and match the builder's verified measurements.

#### G. Sign Expiration Split Accuracy
**Yes.** The subsection *"2 · An expiration date on the live service does nothing"* accurately delineates the client/server split:
- It documents that the server stores `expires_at` on send but lacks any background sweep or server-side expiration enforcement during `/complete`.
- It documents that the initial signing entry point (`ExternalSignView`) allows past-due envelopes to land and send verification codes.
- It documents that the subsequent `SignView` enforces a client-side check after code entry, while noting that the code comment claiming server-side parity is false at `0e26917`.
- It precisely summarizes the failure boundary: *"So the deadline is enforced against the party using the app and not against the party addressing the API directly. This card does not tell you a past-due envelope is unsignable on the live service; it tells you the service is not what stops it."*

---

VERDICT: APPROVE
