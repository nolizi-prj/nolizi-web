# Code review — qwen

**Change:** `content/products/pumasi-booking.md` and
`content/products/pumasi-sign.md` in `pumasi-web`, against `cd1fd2f`.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** qwen, via `openrouter -m qwen/qwen3-max -p -`, prompt on stdin.
**Launched** 2026-09-01 05:51:59 UTC, **answered** by 05:53:48 UTC, exit 0.
**Recruitment order:** claude (skipped — builder) → **gemini** → **qwen** →
**glm** → grok (UNREACHABLE, HTTP 402; not launched, per the packet).

**The diff this reviewer read is provably the diff that was committed.**
The tree was frozen at 05:51:03 UTC — `npm run build` clean (36 files,
exit 0), `npm test` 62/62 — and the diff checksummed:

```console
$ sha256sum /tmp/frozen.diff
f15c9c7e7f6d3bf53d9402361a55bb7dab9d57df63bb5012229ca14d4002101f
$ wc -c /tmp/frozen.diff
43083
```

That checksum was written into the prompt every reviewer received, and the
tree was **re-checksummed after the last verdict landed and still matched**.
No file was edited between the freeze and the commit.

**Prompt contents:** the frozen diff; the nine live-host and source
measurements this seat took between 05:48:45 and 05:50:00 UTC, itemised with
timestamps; the six hard constraints the change is under; and seven lettered
questions — A being whether any sentence lets a skimming reader conclude a
repair has reached users, E whether the re-measured commit count is honest
or noise, and F whether the booking section is proportionate.

**Mechanical note.** Raw capture contained **0 NUL bytes**. Trailing
whitespace normalised; **no word of the review was changed, added or
removed.**

---

A. **No sentence lets a skimming reader conclude that any named repair has reached users.**
Every repair (d7bd490, c000feb, 9659e69, 2471a29) is explicitly paired with its undeployed status. Examples:
- “**The repair is merged and it is not deployed.** [`d7bd490`] … `curl https://booking.pumasi.ai/version` answered `{"version":"0.2.0","commit":"2453adc"}` … and the repair is not in that build.”
- “**Both are merged and undeployed, and this page does not claim either has shipped**” (pumasi-sign limitation).
- “**This page does not claim that repair has shipped**” (repeated for both pumasi-sign defects).
All such statements unambiguously prevent misinterpretation.

B. **No**, the change sets no deadline, names no deployer, and takes no position on Q-012/Q-014/Q-038 defaults. It consistently attributes dates to DECISIONS.md entries:
- “the window that entry's own row records closes 2026-09-08”
- “**That date is [`DECISIONS.md` Q-037]’s own, reported here and not set here**”
- “**this card sets no deadline, names no deployer, and takes no position on their defaults.**”
These comply strictly with constraint 2.

C. **All factual claims are supported by or consistent with the listed measurements.**
- The `/version` responses, slot counts, commit SHAs, grep results, chunk filenames/sizes, and health checks all match the provided measurements.
- The claim that “`git rev-list --count 2453adc..origin/main` … read **seven** half an hour earlier, at 05:21; … **eight** at 05:49” aligns with the measurements (7 → 8).
- The assertion that Zoom is omitted from `/subprocessors` but data is sent to it matches both the curl result (5 parties) and code inspection (`app.ts:3524`).
No contradiction or unsupported claim is present.

D. **Yes, the ordering is defensible.** The empty booking times defect is more severe than the unverified reminder delivery: it blocks core functionality (no bookings can occur), whereas reminders are a post-booking feature. The new lead reflects user-facing impact—“a stranger meets that before anything else”—which aligns with evidence that the page renders zero time buttons despite slots being present. This prioritization is justified by immediacy and severity, not exaggeration.

E. **The commit-count sentence is honest and useful.** It acknowledges mutability (“run the command rather than trusting the number”) and explains why the count changed (active development). It warns readers against treating prose numbers as stable, which is consistent with Pumasi’s ethos of evidence over caching. While the specific numbers will go stale, the instruction to re-run the command makes this self-correcting rather than misleading noise.

F. **The new booking section is proportionate.** It details the defect without hyperbole, grounding every claim in direct measurement (curl, headless Chrome, code inspection). It distinguishes between repository state (fixed) and deployment state (broken), clarifies irrecoverable harm (“bookings that did not happen … are not recoverable”), and avoids blaming language. It neither buries nor inflates—it reports precisely what was observed, including error absence and reproducibility.

G. **One material issue:** In the pumasi-sign limitation field, the phrase “**two defects are live on `sign.pumasi.ai` as this is written**” is followed by a description of the Apache-2.0 license discrepancy as a third issue. But the opening clause says “two defects,” yet three problems are described (message deletion, expiration ineffectiveness, and false licensing claim). However, the text clarifies: the license issue is “separately” discussed and was already present; the “two defects” refer specifically to the newly repaired-but-undeployed functional bugs. This is resolved in the expanded body (“## Two defects are live…”), so the front-matter phrasing is slightly ambiguous but not materially misleading given the full context. No other contradictions or errors found.

VERDICT: APPROVE
