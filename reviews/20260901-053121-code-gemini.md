# Code review — gemini

**Change:** `content/products/pumasi-sign.md` and
`content/products/pumasi-booking.md` in `pumasi-web` — the commons catalog cards
for Pumasi Sign and Pumasi Booking, re-measured first-hand by job `0083` after
adopting job `0078`'s uncommitted working tree, and extended with a defect that
did not exist when `0078` was written.
**Builder family:** claude (`claude-opus-5[1m]`) — reviewer is a different
family, per CHARTER Part 3 requirement 3.
**Reviewer:** gemini, via `agy --dangerously-skip-permissions -p`, prompt passed
as an argv string (this CLI returns a bare greeting on `-p -`).
**Launched** 2026-09-01 05:31:21 UTC, **answered** 05:32 UTC.
**Recruitment order:** claude (skipped — builder) → **gemini** → grok
(UNREACHABLE, HTTP 402, re-probed 05:26 UTC) → qwen → glm → kimi.

**The diff this reviewer read is provably the diff that was committed.** The
working tree was frozen at 05:31:10 UTC — `npm run build` clean, `npm test`
62/62 — and the diff checksummed:

```console
$ sha256sum /tmp/frozen.diff
d6332b6cdc30937f14911f355fe7a1b027e41a98e1dabf7310c5debbea895bde
$ wc -c /tmp/frozen.diff
41838
```

That checksum was written into the prompt every reviewer received, and **no
file was edited between the freeze and the commit.** This is the direct answer
to the two process faults already in this directory: the round in
`…-033413-code-SUPERSEDED-ROUND.md`, where two launches shared three output
paths, and `…-052747-code-ABORTED-ROUND.md`, this job's own aborted round, where
the builder edited the diff after launching.

**Prompt contents:** the frozen diff; every live-host and source measurement
this job took between 05:20:31 and 05:26:39 UTC, itemised with timestamps; the
five hard constraints the change is under; and seven lettered questions — A
being whether any sentence lets a skimming reader conclude a repair has reached
users, and F whether the new booking section is proportionate in both
directions.

**Mechanical note.** Raw capture contained **0 NUL bytes**. Trailing
whitespace normalised; **no word of the review was changed, added or removed.**

---

### Cross-Family Code Review (Pumasi Charter Part 3, Requirement 3)

**Reviewing Family:** Gemini  
**Builder Family:** Claude (`claude-opus-5[1m]`)  
**Target:** `content/products/pumasi-booking.md` and `content/products/pumasi-sign.md` diff against `pumasi-web` `cd1fd2f`.

---

### Evaluation of Explicit Review Questions

#### A. Does any sentence let a skimming reader conclude that any repair named (`d7bd490`, `9659e69`, `2471a29`) has reached users?
**No.** Every mention of a repair is explicitly and repeatedly paired with bolded disclosures stating that the fix is merged in repository source but undeployed on the live host:
- `pumasi-booking.md`: *"...repaired at `d7bd490`, and **not deployed**: `/version` answered `{"version":"0.2.0","commit":"2453adc"}` at 05:20:31 UTC and the repair is not in that build."*
- `pumasi-booking.md`: Section title *"The hosted booking page shows no times, and the repair is merged and not deployed"*; body: *"**This page does not claim that repair has shipped.**"*
- `pumasi-sign.md`: *"**two defects are live on `sign.pumasi.ai` as this is written, both repaired in `main` and neither deployed.**"*
- `pumasi-sign.md`: *"**Both are merged and undeployed, and this page does not claim either has shipped**: nobody owns deployment — `pumasi/DECISIONS.md` **Q-012**, open..."*
- `pumasi-sign.md`: Subsection headers and release note citations explicitly reiterate: *"It is merged and it is **not deployed**."*

#### B. Does every factual claim added by the diff trace to a measurement listed above?
**Yes.** Every single claim maps directly to the first-hand measurements:
1. `GET https://booking.pumasi.ai/yunyoungmok/abc` -> `200`, 45,407 bytes, 24 slots in `#slots-data` at 05:21:07 UTC.
2. `times` methods in served bundle: `times.querySelectorAll` (1) and `times.textContent` (1), zero appends.
3. Headless Chrome run (`America/Chicago`, 05:21:26 UTC): day cells 1 & 2 available, `#picked-day` *Tuesday, September 1*, `#times.children.length = 0`, `#times.innerHTML = ""`, 0 page/console errors.
4. Issue #32 filed 2026-09-01 00:36:56 UTC; `d7bd490` test results 2/6 before, 6/6 after; 331 passing baseline assertions.
5. `/version` response `{"version":"0.2.0","commit":"2453adc"}` at 05:20:31 UTC; `git rev-list --count 2453adc..origin/main` = 7.
6. Subprocessors register: `200`, 34,663 bytes at 05:20:31 UTC naming 5 parties; Zoom disclosures in `service/src/app.ts:3524, 3528` at `2453adc`; presentation hits (`grep -ic zoom` = 4 vs `grep -io "zoom video"` = 0).
7. `sign.pumasi.ai` asset fingerprint `/assets/index-CnoFAC2c.js` mapping to `0e26917` via `STAGE.md` §2.2; `/api/health` 200 at 05:20:33 UTC.
8. Served chunk byte sizes and contents (`EnvelopeDetailView-C4VlFBtA.js` 27,972 bytes; `ExternalSignView-CmDSqiPb.js` 5,250 bytes; `SignView-BM1dkPaZ.js` 46,980 bytes; `LandingView-C5khdw3s.js` 10,046 bytes with 3 Apache-2.0 occurrences).
9. Durables behavior at `0e26917`: `durable.ts:1209-1211` (`NULL` overwrite on omitted message), `durable.ts:1214` audit row with no `details_json`, `durable.ts:1062` (`expires_at` INSERT), missing `crons`/`scheduled` in `wrangler.jsonc`/`worker.ts`.
10. License checks: `gh api repos/pumasi-ai/pumasi-sign/contents/LICENSE` -> 404 (05:22:52 UTC); `licenseInfo` detection status across repos (05:26:39 UTC).

#### C. Are the Q-012/Q-021/Q-035/Q-036/Q-037/Q-038 references reporting state rather than setting deadlines?
**Yes.** All governance citations explicitly attribute deadlines and status to the respective entries in `DECISIONS.md`:
- Q-035, Q-037, Q-038 dates are reported as *"the window that entry's own row records closes 2026-09-08"* and *"reported here and not set here — this card sets no deadline on any steward entry and has no standing to."*
- Q-012, Q-021, and Q-036 are correctly reported as open questions reserved for the steward.

#### D. Are the line numbers, byte counts, commit SHAs and counts in the diff internally consistent and consistent with the measurements above?
**Yes.** All citations match:
- SHAs: `2453adc`, `d7bd490`, `50f911f`, `c000feb`, `c4b1159`, `0e26917`, `9659e69`, `2471a29`, `3f51fa1`, `f0d1912`, `3edd06f`, `6e02cc4`, `14d8af4`, `df8f0d0`.
- Commit counts: 7 commits ahead for booking (`2453adc..origin/main`); 9 commits ahead for sign (`0e26917..f0d1912`), 2 touching `durable.ts`.
- Line references: `pages.ts` :771, :974, :985; `app.ts` :3524, :3528, :3533-3545, :3549-3557; `durable.ts` :1062, :1209-1211, :1214, :1326; `SendView.vue` :1300, :1332; `EnvelopeDetailView.vue` :1104; `SignView.vue` :480.
- All chunk byte counts and HTTP response codes match measurement data.

#### E. Current state of `STAGE.md` description
**Accurate.** The replacement text confirms `STAGE.md` was corrected at `3f51fa1` (with approvals at `f0d1912`), accurately quotes the updated Stage 1 row (*"IN PROGRESS — NOT MET, and the unmet half is Surface B's honesty, not its existence"*), makes no claim of credit, and notes that the exit gate remains `NOT MET`.

#### F. Booking section proportionality
**Proportionate.** The text accurately characterizes the failure as universal on the deployed build (due to `bookingPage()` in `service/src/pages.ts`), notes the absence of client/server exceptions, accurately describes the user experience (empty time picker), cites the root cause and test evidence, notes that past unmade bookings cannot be recovered, and confirms that local clones of `main` build cleanly and show slots.

#### G. Factually wrong, internally contradictory, or stale elements
**None found.** The diff adheres strictly to all 5 hard constraints, does not name any individual person, attributes all dates and claims to measurements or governance entries, and maintains complete internal consistency.

---

VERDICT: APPROVE
